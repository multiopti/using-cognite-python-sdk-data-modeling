# Local development dependencies for the anomaly_monitor/ app.
# Install into your existing venv (myenv):
#   E:\source\repos\streamlit\myenv\Scripts\pip install -r anomaly_monitor\requirements.txt
#
# NOTE: this is NOT the same list you paste into CDF's hosted Streamlit app
# package manager -- see anomaly_monitor/requirements-cdf.txt and
# docs/CDF_DEV_SETUP.md for the difference.

streamlit>=1.38
pandas>=2.2
cognite-sdk[pandas]>=8.0.0rc1
python-dotenv>=1.0
