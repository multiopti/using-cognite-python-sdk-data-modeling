# Local development dependencies for the streamlit/ app.
# Install into your existing venv (myenv):
#   E:\source\repos\streamlit\myenv\Scripts\pip install -r streamlit\requirements.txt
#
# NOTE: this is NOT the same list you paste into CDF's hosted Streamlit app
# package manager. That environment runs on Pyodide (in-browser, WebAssembly)
# and only accepts pure-Python packages -- see streamlit/requirements-cdf.txt
# and docs/CDF_DEV_SETUP.md for the difference.

streamlit>=1.38
pandas>=2.2
numpy>=1.26
plotly>=5.22
cognite-sdk[pandas]>=8.0.0rc1
python-dotenv>=1.0
